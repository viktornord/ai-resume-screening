# Resume Screening UI

React 18 + TypeScript + Vite + Tailwind (shadcn-style components).

## Dev

```bash
npm install
npm run dev
```

Ensure the API runs on port 8000 (or set `VITE_API_BASE`).

## Build

```bash
npm run build
npm run preview
```
